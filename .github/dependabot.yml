# Keeps the SHA-pinned actions in .github/workflows current. Daily, one grouped PR.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"
    assignees:
      - "em-lorien"
    groups:
      actions:
        patterns:
          - "*"
